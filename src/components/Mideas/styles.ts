import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    align-items: center;
    padding: 0 20px;
    width: 200px;
    height: 50px;
    font-weight: bold;
    border-radius: 20px;
    background: var(--purple);
    color: var(--white);
`