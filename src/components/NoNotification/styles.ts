import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    align-items: center;

    img {
        margin-left: 5px;
    }

    h2 {
        font-size: 18px;
        font-weight: bold;
        color: var(--gray);
    }
`